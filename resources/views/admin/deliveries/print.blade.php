<!doctype html><html><head><meta charset="utf-8"><title>{{ $delivery->delivery_number }}</title><link rel="stylesheet" href="{{ asset('css/delivery.css') }}"></head><body class="dlv-print-body"><div class="dlv-document"><header><div><div class="dlv-doc-brand"><span>J</span><div><strong>JALVAN</strong><small>Custom Branded Water Bottles</small></div></div></div><div class="dlv-doc-title"><span>DELIVERY CHALLAN</span><strong>{{ $delivery->delivery_number }}</strong><small>{{ $delivery->delivery_date?->format('d M Y') }}</small></div></header><div class="dlv-doc-meta"><div><span>DELIVER TO</span><strong>{{ $delivery->customer->business_name }}</strong><small>{{ $delivery->delivery_address }}</small><small>{{ collect([$delivery->delivery_city,$delivery->delivery_state,$delivery->delivery_pincode])->filter()->implode(', ') }}</small></div><div><span>ORDER</span><strong>{{ $delivery->order->order_number }}</strong><small>Status: {{ ucwords(str_replace('_',' ',$delivery->status)) }}</small></div><div><span>DRIVER</span><strong>{{ $delivery->driver_name ?: '—' }}</strong><small>{{ $delivery->vehicle_number ?: '' }}</small></div></div><table><thead><tr><th>#</th><th>Product</th><th>Batch</th><th>Qty</th><th>Unit</th></tr></thead><tbody>@foreach($delivery->items as $n=>$i)<tr><td>{{ $n+1 }}</td><td>{{ $i->product?->name ?? $i->description }}</td><td>{{ $i->batch?->batch_number ?? '—' }}</td><td>{{ rtrim(rtrim(number_format($i->quantity,3),'0'),'.') }}</td><td>{{ $i->unit }}</td></tr>@endforeach</tbody></table><div class="dlv-doc-bottom"><div><h4>Receiver</h4><p>Name: {{ $delivery->receiver_name ?: '________________' }}<br>Mobile: {{ $delivery->receiver_mobile ?: '________________' }}<br>Signature: ________________________</p></div><div><h4>Notes</h4><p>{{ $delivery->notes ?: 'No notes.' }}</p></div></div><footer>Thank you for your business. · JALVAN ERP</footer></div><script>window.onload=()=>window.print()</script></body></html>
