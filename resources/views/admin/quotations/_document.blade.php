<div class="qt-document"><div class="qt-doc-head"><div><div class="qt-doc-brand">JALVAN</div><div class="qt-doc-tag">CUSTOM BRANDED WATER BOTTLES</div></div><div class="qt-doc-meta"><strong>{{ $quotation->quotation_number }}</strong><span>Date: {{ $quotation->quotation_date?->format('d M Y') }}</span><span>Valid until: {{ $quotation->valid_until?->format('d M Y') ?? 'No expiry' }}</span></div></div>
<div class="qt-doc-parties"><div><span>QUOTATION FOR</span><strong>{{ $quotation->customer->business_name }}</strong><small>{{ $quotation->customer->customer_code }}</small><small>{{ $quotation->customer->address }}{{ $quotation->customer->city ? ', '.$quotation->customer->city : '' }}</small></div><div><span>PREPARED BY</span><strong>{{ $quotation->creator?->name ?? 'JALVAN' }}</strong><small>{{ $quotation->created_at?->format('d M Y') }}</small></div></div>
<table class="qt-doc-table"><thead><tr><th>Product</th><th>Description</th><th>Qty</th><th>Unit Price</th><th>Total</th></tr></thead><tbody>@foreach($quotation->items as $item)<tr><td><strong>{{ $item->product?->name ?? 'Custom item' }}</strong></td><td>{{ $item->description }}</td><td>{{ rtrim(rtrim(number_format($item->quantity,3), '0'), '.') }} {{ $item->unit }}</td><td>₹{{ number_format($item->unit_price,2) }}</td><td><strong>₹{{ number_format($item->line_total,2) }}</strong></td></tr>@endforeach</tbody></table>
<div class="qt-doc-total"><div><span>Subtotal</span><strong>₹{{ number_format($quotation->subtotal,2) }}</strong></div><div><span>Discount</span><strong>− ₹{{ number_format($quotation->discount_amount,2) }}</strong></div><div><span>Tax</span><strong>₹{{ number_format($quotation->tax_amount,2) }}</strong></div><div><span>Shipping / Other</span><strong>₹{{ number_format($quotation->shipping_amount+$quotation->other_amount,2) }}</strong></div><div class="qt-doc-grand"><span>Grand Total</span><strong>₹{{ number_format($quotation->grand_total,2) }}</strong></div></div>
<div class="qt-doc-notes"><div><span>NOTES</span><p>{{ $quotation->notes ?: '—' }}</p></div><div><span>TERMS & CONDITIONS</span><p>{!! nl2br(e($quotation->terms_conditions ?: '—')) !!}</p></div></div><div class="qt-doc-footer">Thank you for your business. · JALVAN Custom Branded Water Bottles</div></div>
