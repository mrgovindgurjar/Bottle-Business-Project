@extends('layouts.admin')
@section('title',$product->name)
@section('page_title','Product')
@section('breadcrumb')Catalog / Products / {{ $product->sku }}@endsection
@section('content')
<div class="product-page-shell">
    <div class="product-detail-hero">
        <div class="product-detail-visual"><div class="product-detail-image">@if($product->image_url)<img src="{{ $product->image_url }}" alt="{{ $product->name }}">@else<span>{{ strtoupper(substr($product->name,0,1)) }}</span>@endif</div><div class="product-detail-visual-copy"><span class="product-status {{ $product->status==='active'?'is-active':'is-inactive' }}"><b></b>{{ ucfirst($product->status) }}</span><small>{{ $product->is_custom_branding ? 'CUSTOM BRANDING READY' : 'STANDARD PRODUCT' }}</small></div></div>
        <div class="product-detail-main"><div class="eyebrow">{{ $product->sku }}</div><h1>{{ $product->name }}</h1><p>{{ $product->short_description ?: 'No short description has been added yet.' }}</p><div class="product-detail-actions"><a href="{{ route('admin.products.edit',$product) }}" class="btn btn-primary">Edit Product</a><a href="{{ route('admin.pricing.index',['search'=>$product->sku]) }}" class="btn btn-secondary">Manage Pricing</a>@if(auth()->user()->hasPermission('products.delete') && $product->status==='active')<form method="POST" action="{{ route('admin.products.destroy',$product) }}" data-confirm-form data-confirm-message="Deactivate this product?">@csrf @method('DELETE')<button class="btn btn-danger-outline" type="submit">Deactivate</button></form>@endif</div></div>
    </div>

    <div class="product-detail-grid">
        <div class="product-detail-left">
            <div class="card product-info-card"><div class="product-card-head"><div><div class="eyebrow">SPECIFICATION</div><h3>Product Details</h3></div></div><div class="product-detail-spec-grid">
                <div><span>Bottle Size</span><strong>{{ number_format($product->bottle_size_ml) }} ml</strong></div><div><span>Bottle Type</span><strong>{{ $product->bottle_type ?: '—' }}</strong></div><div><span>Material</span><strong>{{ $product->material ?: '—' }}</strong></div><div><span>Cap Type</span><strong>{{ $product->cap_type ?: '—' }}</strong></div><div><span>Label Type</span><strong>{{ $product->label_type ?: '—' }}</strong></div><div><span>Units / Box</span><strong>{{ number_format($product->units_per_box) }}</strong></div><div><span>Selling Unit</span><strong>{{ ucfirst($product->unit) }}</strong></div><div><span>Branding</span><strong>{{ $product->is_custom_branding ? 'Custom' : 'Standard' }}</strong></div>
            </div>@if($product->description)<div class="product-description"><span>Internal Description</span><p>{{ $product->description }}</p></div>@endif</div>

            <div class="card product-pricing-card"><div class="product-card-head"><div><div class="eyebrow">COMMERCIAL RULES</div><h3>Pricing</h3><span>{{ $priceStats['active'] }} active rules · {{ $priceStats['customer'] }} customer-specific</span></div></div>
                @if(auth()->user()->hasPermission('pricing.create') || auth()->user()->hasPermission('pricing.edit'))
                <form method="POST" action="{{ route('admin.products.prices.store',$product) }}" class="inline-price-form" data-price-form>@csrf<div class="inline-price-title">Add pricing rule</div><div class="inline-price-grid"><select name="customer_id" class="form-control"><option value="">Global price</option>@foreach(\App\Models\Customer::where('status','active')->orderBy('business_name')->get() as $customer)<option value="{{ $customer->id }}">{{ $customer->business_name }}</option>@endforeach</select><select name="pricing_type" class="form-control"><option value="standard">Standard</option><option value="bulk">Bulk</option><option value="recurring">Recurring</option><option value="custom">Custom</option></select><input class="form-control" type="number" min="1" name="min_quantity" value="1" placeholder="Min qty"><input class="form-control" type="number" min="1" name="max_quantity" placeholder="Max qty"><div class="price-input"><span>₹</span><input class="form-control" type="number" min="0" step="0.01" name="unit_price" placeholder="Unit price" required></div><input class="form-control" type="date" name="effective_from"><input class="form-control" type="date" name="effective_to"><button class="btn btn-primary" type="submit">Add Price</button></div></form>
                @endif
                <div class="price-rules-list">
                @forelse($product->prices as $price)
                    <div class="price-rule-row {{ $price->status==='inactive' ? 'inactive' : '' }}"><div class="price-rule-icon">₹</div><div class="price-rule-main"><strong>{{ ucfirst($price->pricing_type) }}</strong><span>{{ $price->customer?->business_name ?: 'Global pricing' }}</span></div><div class="price-rule-range"><strong>{{ $price->min_quantity }}{{ $price->max_quantity ? '–'.$price->max_quantity : '+' }}</strong><span>bottles</span></div><div class="price-rule-value"><strong>₹{{ number_format((float)$price->unit_price,2) }}</strong><span>/ {{ $product->unit }}</span></div><div><span class="price-rule-status">{{ ucfirst($price->status) }}</span></div><div class="price-rule-actions">@if(auth()->user()->hasPermission('pricing.edit'))<button type="button" class="icon-action" data-price-edit-toggle="#price-edit-{{ $price->id }}">Edit</button>@endif @if($price->status==='active' && auth()->user()->hasPermission('pricing.delete'))<form method="POST" action="{{ route('admin.products.prices.destroy',$price) }}" data-confirm-form data-confirm-message="Deactivate this pricing rule?"><input type="hidden" name="_token" value="{{ csrf_token() }}">@method('DELETE')<button class="icon-action danger" title="Deactivate">×</button></form>@endif</div></div><div class="price-edit-panel" id="price-edit-{{ $price->id }}"><form method="POST" action="{{ route('admin.products.prices.update',$price) }}">@csrf @method('PUT')<div class="inline-price-grid"><select name="customer_id" class="form-control"><option value="">Global price</option>@foreach(\App\Models\Customer::where('status','active')->orderBy('business_name')->get() as $customer)<option value="{{ $customer->id }}" @selected((string)$price->customer_id===(string)$customer->id)>{{ $customer->business_name }}</option>@endforeach</select><select name="pricing_type" class="form-control"><option value="standard" @selected($price->pricing_type==='standard')>Standard</option><option value="bulk" @selected($price->pricing_type==='bulk')>Bulk</option><option value="recurring" @selected($price->pricing_type==='recurring')>Recurring</option><option value="custom" @selected($price->pricing_type==='custom')>Custom</option></select><input class="form-control" type="number" min="1" name="min_quantity" value="{{ $price->min_quantity }}"><input class="form-control" type="number" min="1" name="max_quantity" value="{{ $price->max_quantity }}"><div class="price-input"><span>₹</span><input class="form-control" type="number" min="0" step="0.01" name="unit_price" value="{{ $price->unit_price }}" required></div><input class="form-control" type="date" name="effective_from" value="{{ $price->effective_from?->format('Y-m-d') }}"><input class="form-control" type="date" name="effective_to" value="{{ $price->effective_to?->format('Y-m-d') }}"><select name="status" class="form-control"><option value="active" @selected($price->status==='active')>Active</option><option value="inactive" @selected($price->status==='inactive')>Inactive</option></select><button class="btn btn-primary" type="submit">Save</button></div></form></div>
                @empty<div class="price-empty"><span>₹</span><strong>No pricing rules yet</strong><small>Add global or customer-specific pricing to make this product commercially ready.</small></div>@endforelse
                </div>
            </div>
        </div>
        <aside class="product-detail-side"><div class="card product-side-card"><div class="eyebrow">QUICK SUMMARY</div><div class="product-side-stat"><span>Active price rules</span><strong>{{ $priceStats['active'] }}</strong></div><div class="product-side-stat"><span>Global rules</span><strong>{{ $priceStats['standard'] }}</strong></div><div class="product-side-stat"><span>Customer rules</span><strong>{{ $priceStats['customer'] }}</strong></div></div><div class="card product-side-card"><div class="eyebrow">NEXT WORKFLOW</div><h3>Ready for the next modules?</h3><p>Products will feed quotations, orders, recurring plans, inventory and batch operations.</p><div class="product-flow"><span>Product</span><i>↓</i><span>Quotation</span><i>↓</i><span>Order</span><i>↓</i><span>Production</span></div></div></aside>
    </div>
</div>
@endsection
