@extends('layouts.admin')
@push('styles')<link rel="stylesheet" href="{{ asset('css/staff-attendance.css') }}">@endpush
@section('content')<div class="hr-head"><div><div class="hr-kicker">ATTENDANCE</div><h1>Edit Attendance</h1><p>{{ $attendance->staff->full_name }} · {{ $attendance->attendance_date->format('d M Y') }}</p></div></div>@if($errors->any())<div class="hr-alert hr-error">{{ $errors->first() }}</div>@endif<form class="hr-card" method="POST" action="{{ route('admin.attendance.update',$attendance) }}">@csrf @method('PUT')<div class="hr-fields"><label>Status<select name="status">@foreach(['present','absent','late','half_day','leave','holiday'] as $s)<option value="{{ $s }}" @selected($attendance->status===$s)>{{ ucwords(str_replace('_',' ',$s)) }}</option>@endforeach</select></label><label>Check In<input type="time" name="check_in" value="{{ $attendance->check_in }}"></label><label>Check Out<input type="time" name="check_out" value="{{ $attendance->check_out }}"></label><label class="hr-span-2">Remarks<textarea name="remarks">{{ $attendance->remarks }}</textarea></label></div><div class="hr-form-footer"><a class="hr-btn" href="{{ route('admin.attendance.index',['date'=>$attendance->attendance_date->toDateString()]) }}">Cancel</a><button class="hr-btn hr-btn-primary">Update Attendance</button></div></form>@endsection
